{{#if success_msg}}
<div id="msgContainer">
    <div class="msg_success show" id="msg">
        <div class="msg-header">
            {{success_msg}}
            {{!-- <button type="button" class="close">
                <span aria-hidden="true">&times;</span>
            </button> --}}
        </div>
    </div>
</div>
{{/if}}

{{#if error_msg}}
<div id="msgContainer">
    <div class="msg_error show" id="msg">
        <div class="msg-header">
            {{error_msg}}
            {{!-- <button type="button" class="close">
                <span aria-hidden="true">&times;</span>
            </button> --}}
        </div>
    </div>
</div>
{{/if}}